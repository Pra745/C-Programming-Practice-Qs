#include <stdio.h>
void main()
{
    printf("Enter a year");
    int year=0;
    scanf("%d",&year);
    if (year%4==0)
    printf("%d is a leap year",year);
    else
    printf("%d is not a leap year",year);
}
